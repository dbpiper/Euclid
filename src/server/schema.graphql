type Query {
  stock(stockId: ID!): Stock
  stocks(stockListTicker: String!): [String!]!
}

type Mutation {
  createStock(date: Int!, price: Float!, ticker: String!): Stock
}

type Stock {
  id: ID!
  price: Float!
  date: Int!
  ticker: String!
}
